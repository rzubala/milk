class Poo {
    id: string
    timestamp: number
    count: number   

    constructor(id: string, timestamp: number, count: number ) {
        this.id = id
        this.timestamp = timestamp
        this.count = count
    }
}

export default Poo