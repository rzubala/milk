class Feeding {
    id: string
    date: Date
    volume: number

    constructor(id: string, date: Date, volume: number) {
        this.id = id
        this.date = date
        this.volume = volume
    }
}

export default Feeding